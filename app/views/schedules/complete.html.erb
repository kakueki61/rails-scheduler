<div class="container-fluid">

  <p>以下のスケジュールを登録しました。</p>
  <table class="table table-hover">
    <thead>
    <tr>
      <th scope="row">日付</th>
      <th scope="row">開始時間</th>
      <th scope="row">終了時間</th>
    </tr>
    </thead>
    <tbody>
    <% @saved_schedules.each do |schedule| %>
      <tr>
        <th scope="row"><%= schedule.start_at.strftime("%m/%d(%a)") %></th>
        <td><%= schedule.start_at.strftime("%H:%M") %></td>
        <td><%= schedule.end_at.strftime("%H:%M") %></td>
      </tr>
    <% end %>
    </tbody>
  </table>

</div>

