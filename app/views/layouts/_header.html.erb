<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <%#= link_to "sample app", root_path, id: "logo" %>
<!--      <a class="navbar-brand" href="#">Navbar</a>-->
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="true" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Home", root_path, class: 'nav-link' %>
          </li>
          <% if logged_in? %>
            <li class="nav-item">
              <%= link_to "Worker", workers_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to "スケジュール", new_schedule_path, class: 'nav-link' %>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Account
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                <%= link_to "Profile", workers_path, class: 'dropdown-item' %>
                <%= link_to "Settings", edit_worker_registration_path(current_worker), class: 'dropdown-item' %>
                <%= link_to "Log out", destroy_worker_session_path, method: :delete, class: 'dropdown-item' %>
              </div>
            </li>
          <% else %>
            <li><%= link_to "Log in", new_worker_session_path, class: 'nav-link' %></li>
          <% end %>
        </ul>
      </div>
    </nav>
  </div>
</header>